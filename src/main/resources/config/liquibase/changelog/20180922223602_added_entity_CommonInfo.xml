<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.5.xsd
                        http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

    <property name="now" value="now()" dbms="h2"/>

    <property name="now" value="now()" dbms="mysql"/>
    <property name="autoIncrement" value="true"/>

    <property name="floatType" value="float4" dbms="postgresql, h2"/>
    <property name="floatType" value="float" dbms="mysql, oracle, mssql"/>

    <!--
        Added the entity CommonInfo.
    -->
    <changeSet id="20180922223602-1" author="jhipster">
        <createTable tableName="common_info">
            <column name="id" type="bigint" autoIncrement="${autoIncrement}">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="title" type="varchar(255)" remarks="the title">
                <constraints nullable="true" />
            </column>

            <column name="description" type="varchar(255)" remarks="the description of the info">
                <constraints nullable="true" />
            </column>

            <column name="show_start_date" type="date" remarks="the duration start to show the info">
                <constraints nullable="true" />
            </column>

            <column name="show_end_date" type="date" remarks="the duration end to hide the info">
                <constraints nullable="true" />
            </column>

            <column name="alarm_relevant" type="bit" remarks="is this info relevant for alarms in the given duration">
                <constraints nullable="true" />
            </column>

            <column name="alarm_relevant_start_date" type="date" remarks="the duration start of an alarm relevant info">
                <constraints nullable="true" />
            </column>

            <column name="alarm_relevant_end_date" type="date" remarks="the duration end of an alarm relevant info">
                <constraints nullable="true" />
            </column>

            <!-- jhipster-needle-liquibase-add-column - JHipster will add columns here, do not remove-->
        </createTable>
        
    </changeSet>
    <!-- jhipster-needle-liquibase-add-changeset - JHipster will add changesets here, do not remove-->
</databaseChangeLog>
